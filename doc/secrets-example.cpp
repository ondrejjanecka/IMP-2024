// secrets.cpp

// This file is an example of the secrets.h file that you need to create in the same directory as the main.cpp file.

#include "secrets.h"

const char* ssid = "wifi_name";
const char* password = "wifi_password";

const char* mqttServer = "mqtt_server_address";
const char* mqttAccount = "mqtt_user_name";
const char* mqttPassword = "mqtt_user_password";
